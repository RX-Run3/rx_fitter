brem:
  0: int(L1_HASBREMADDED_brem_track_2) + int(L2_HASBREMADDED_brem_track_2) == 0
  1: int(L1_HASBREMADDED_brem_track_2) + int(L2_HASBREMADDED_brem_track_2) == 1
  2: int(L1_HASBREMADDED_brem_track_2) + int(L2_HASBREMADDED_brem_track_2) >= 2
selection:
  mva : (mva_cmb > 0) && (mva_prc > 0)
fitting:
  config:
    Bu_JpsiK_ee_eq_DPC:
      cfg_kde:
        bandwidth: 20
        padding:
          lowermirror: 0.5
          uppermirror: 0.5
      plotting:
        nbins: 30
        stacked: true
    Bu_psi2SK_ee_eq_DPC:
      cfg_kde:
        bandwidth: 20
        padding:
          lowermirror: 0.5
          uppermirror: 0.5
      plotting:
        nbins: 30
        stacked: true
input:
  selection:
  trigger: Hlt2RD_BuToKpEE_MVA
output:
  out_dir: /home/acampove/external_ssd/fits/data/
